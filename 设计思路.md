做成算子库的形式：使用python调用
util.lib::loadbinary 解析hsaco, 得到 kernelfuncaddr,mod,n_regs,n_spills ，调用hipModuleLoadData载入内存 ；
之后使用对应算子的 driver.lib的 launch 方法 + funcaddr以及其他参数 运行kernel
hipModuleLaunchKernel(function, gridX, gridY, gridZ, 64*num_warps, 1, 1, shared_memory, stream, params, 0)
params为参数数组的指针

loadbinary是通用的; driver.lib 具有特殊性(kernel参数类型、数目、顺序不同，用户决定)

